# Docker port mapping
BACKEND_PORT=8800

# Spring Boot
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8800
TZ=Asia/Shanghai
JAVA_TOOL_OPTIONS=-Xms512m -Xmx1024m -Dfile.encoding=UTF-8 -Duser.timezone=Asia/Shanghai
LOG_PATH=/app/logs
LOG_RETENTION=7d

# MySQL. Use host.docker.internal when MySQL runs on this Docker host.
MYSQL_HOST=host.docker.internal
MYSQL_USER=nexora
MYSQL_PASSWORD=CHANGE_ME

# Redis. Use host.docker.internal when Redis runs on this Docker host.
REDIS_HOST=host.docker.internal
REDIS_PASSWORD=CHANGE_ME

# Response encryption key. Only needed when an endpoint uses @EncryptResponse.
# Generate a Base64-encoded 32-byte key, for example: openssl rand -base64 32
PLATFORM_RESPONSE_SECRET_KEY=CHANGE_TO_A_BASE64_ENCODED_32_BYTE_KEY

# Server-side credential encryption key for mail authorization codes and saved SSH passwords.
# Generate a separate Base64-encoded 32-byte key.
PLATFORM_CREDENTIAL_SECRET_KEY=CHANGE_TO_A_BASE64_ENCODED_32_BYTE_KEY

# Qiniu Kodo
OSS_QINIU_ACCESS_KEY=CHANGE_ME
OSS_QINIU_SECRET_KEY=CHANGE_ME
OSS_QINIU_BUCKET_NAME=nexora-prod
OSS_QINIU_DOMAIN=https://oss.example.com/
OSS_QINIU_BASE_PATH=base/

# Production API documentation (disabled by default)
KNIFE4J_ENABLE=false
KNIFE4J_BASIC_ENABLE=false
KNIFE4J_BASIC_USERNAME=admin
KNIFE4J_BASIC_PASSWORD=CHANGE_ME
SPRINGDOC_API_DOCS_ENABLED=false
SPRINGDOC_SWAGGER_UI_ENABLED=false
