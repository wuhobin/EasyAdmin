<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexora.identity.mapper.SysUserMapper">

    <resultMap type="com.nexora.identity.domain.vo.user.SysUserPageListVo" id="SysUserPageResultVoMap">
        <id property="id" column="id" />
        <result property="status" column="status" />
        <result property="lastLoginTime" column="last_login_time" />
        <result property="ip" column="ip" />
        <result property="ipLocation" column="ip_location" />
        <result property="nickname" column="nickname" />
        <result property="avatar" column="avatar" />
        <result property="mobile" column="mobile" />
        <result property="email" column="email" />
        <result property="sex" column="sex" />
        <result property="createTime" column="create_time" />

    </resultMap>

    <resultMap type="com.nexora.identity.domain.vo.user.SysUserPageListVo" id="SysUserRoleResultMap">
        <id property="id" column="user_id"/>
        <collection property="roleIds" ofType="int">
            <result column="role_id"/>
        </collection>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, password, create_time, update_time, status,
        ip, ip_location, os, last_login_time, browser,
        nickname, avatar, mobile, email, sex, login_type
    </sql>

    <!-- 根据邮箱查询用户信息 -->
    <select id="selectByEmail" resultType="com.nexora.identity.entity.SysUser">
        select
        <include refid="Base_Column_List"/>
        from sys_user
        where email = #{email}
    </select>

    <select id="selectUserPage" resultMap="SysUserPageResultVoMap">
        select
            u.id,
            u.create_time,
            u.status,
            u.last_login_time,
            u.ip,
            u.ip_location,
            u.nickname,
            u.avatar,
            u.mobile,
            u.email,
            u.sex
        from sys_user u
        ${ew.customSqlSegment}
    </select>

    <select id="selectUserRoles" resultMap="SysUserRoleResultMap">
        select user_id, role_id
        from sys_user_role
        where user_id in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </select>
</mapper>
